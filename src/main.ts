import './scss/index.scss';

const app = document.getElementById('app');

const message = 'possibility expand infinitely';

const paragraph = document.createElement('p');
paragraph.innerText = message;

app.appendChild(paragraph);
